# Development and test dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
httpx>=0.27          # required by fastapi.testclient
ruff>=0.4

# Used only by tests/integration to render Arabic sample pages correctly:
# without joining and the bidi algorithm the glyphs render isolated and in
# logical order, which is not what a real page looks like.
arabic-reshaper>=3.0
python-bidi>=0.4
Pillow>=10.0
psutil>=5.9
